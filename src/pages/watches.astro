---
import Layout from '../layouts/Layout.astro';
import { PublicCatalog } from '../components/public-catalog';
import { generatePageMeta } from '../utils/seo';
const meta = generatePageMeta('watches');
---

<Layout {...meta}>
  <PublicCatalog client:load />
</Layout>
