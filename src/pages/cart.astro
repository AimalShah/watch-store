---
import Layout from '../layouts/Layout.astro';
import { CartPage } from '../components/cart-page';
import { generatePageMeta } from '../utils/seo';
const meta = generatePageMeta('cart');
---

<Layout {...meta}>
  <CartPage client:load />
</Layout>
